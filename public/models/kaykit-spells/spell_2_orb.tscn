[gd_scene format=3 uid="uid://blwfe02xwiedv"]

[ext_resource type="Texture2D" uid="uid://bnwfvpekx6thj" path="res://textures/noise_04.png" id="1_wk6d5"]
[ext_resource type="Texture2D" uid="uid://6gmei8riyawp" path="res://textures/gradient_radial_01.png" id="2_582q2"]
[ext_resource type="Texture2D" uid="uid://2pj3e1fqcbab" path="res://textures/noise_07.png" id="3_i1ty8"]
[ext_resource type="Texture2D" uid="uid://btf4w568n436w" path="res://textures/gradient_radial_02.png" id="4_gm0l3"]
[ext_resource type="Texture2D" uid="uid://cxype6esqxhyf" path="res://textures/flare_03.png" id="5_xpi13"]

[sub_resource type="GDScript" id="GDScript_kyo84"]
script/source = "@tool
extends Node3D

@export var effect_container: NodePath

var alive := 0

var list := [
	[ \"Fire Color\", [
		{ \"node_path\": \"Fire\", \"object_path\": \"draw_pass_1\", \"sub_object_path\": \"material\", \"property\": \"shader_parameter/gradient\" },
	]],
	[ \"Glow Color\", [
		{ \"node_path\": \"Glow\", \"object_path\": \"process_material\", \"property\": \"shader_parameter/initial_color\" },
	]],
	[ \"Spark Color\", [
		{ \"node_path\": \"Sparks\", \"object_path\": \"process_material\", \"property\": \"shader_parameter/initial_color\" },
	]],
	[ \"Flare Color\", [
		{ \"node_path\": \"Flare\", \"object_path\": \"process_material\", \"property\": \"shader_parameter/initial_color\" },
	]],
]

func _ready() -> void:
	if Engine.is_editor_hint():
		return
	var container: Node = self
	if effect_container:
		container = get_node(effect_container)
	for n in container.get_children():
		if n is GPUParticles3D:
			if n.has_meta(\"delay\"):
				get_tree().create_timer(n.get_meta(\"delay\")).timeout.connect(start.bind(n))
			else:
				start(n)
			alive += 1

func start(node: GPUParticles3D) -> void:
	node.restart()
	node.finished.connect(on_finished)

func on_finished() -> void:
	alive -= 1
	if alive <= 0:
		queue_free()

func stop(node: Node = null) -> void:
	if !node:
		node = self
	if node is GPUParticles3D:
		node.emitting = false
	for c in node.get_children():
		stop(c)

func _get_property_list() -> Array[Dictionary]:
	if !is_inside_tree() or !Engine.is_editor_hint():
		return []
	var l: Array[Dictionary]
	for group in list:
		for prop in group[1]:
			var node: Node = self
			if prop.has(\"node_path\"):
				node = get_node(prop.node_path)
				if !node:
					print(\"Node not found %s\" % prop.node_path)
					continue
			var obj: Object = node
			if prop.has(\"object_path\"):
				obj = node.get(prop.object_path)
				if !obj:
					print(\"Object not found %s\" % prop.object_path)
					continue
				if prop.has(\"sub_object_path\"):
					obj = obj.get(prop.sub_object_path)
					if !obj:
						print(\"Sub-object not found %s\" % prop.sub_object_path)
						continue
			if obj == self:
				continue
			var prop_list = obj.get_property_list()
			var done := false
			for p in prop_list:
				if p.name == prop.property:
					var property = p.duplicate()
					property.name = group[0]
					l.push_back(property)
					done = true
					break
			if done:
				break
	return l

func _get(property: StringName) -> Variant:
	if !is_inside_tree() or !Engine.is_editor_hint():
		return null
	var r := find_property(property)
	if !r.is_empty():
		return r.object.get(r.property)
	else:
		return null

func _set(property: StringName, value: Variant) -> bool:
	if !is_inside_tree() or !Engine.is_editor_hint():
		return false
	for group in list:
		if group[0] != property:
			continue
		for prop in group[1]:
			var node: Node = self
			if prop.has(\"node_path\"):
				node = get_node(prop.node_path)
				if !node:
					continue
			var obj: Object = node
			if prop.has(\"object_path\"):
				obj = node.get(prop.object_path)
				if !obj:
					continue
				if prop.has(\"sub_object_path\"):
					obj = obj.get(prop.sub_object_path)
					if !obj:
						continue
			obj.set(prop.property, value)
	return false

func find_property(property: StringName) -> Dictionary:
	for group in list:
		if property != group[0]:
			continue
		for prop in group[1]:
			var node: Node = self
			if prop.has(\"node_path\"):
				node = get_node(prop.node_path)
				if !node:
					continue
			var obj: Object = node
			if prop.has(\"object_path\"):
				obj = node.get(prop.object_path)
				if !obj:
					continue
				if prop.has(\"sub_object_path\"):
					obj = obj.get(prop.sub_object_path)
					if !obj:
						continue
			return { \"node\": node, \"object\": obj, \"property\": prop.property }
	return {}
"

[sub_resource type="Gradient" id="Gradient_c0xdh"]
offsets = PackedFloat32Array(0, 0.30604982, 0.66548043, 0.9466192, 1)
colors = PackedColorArray(0, 0, 0, 1, 0.61, 0.210755, 0.14030002, 1, 0.85, 0.61979175, 0.2975, 1, 0.95, 0.820325, 0.5795, 1, 1, 0.98, 0.9, 1)

[sub_resource type="GradientTexture1D" id="GradientTexture1D_k04dp"]
gradient = SubResource("Gradient_c0xdh")

[sub_resource type="Shader" id="Shader_ryygs"]
code = "shader_type particles;
render_mode disable_velocity;

const float DEG_TO_RAD = PI / 180.0;
const uint AMOUNT = uint(64);

struct Particle {
	uint seed;
	float delta;
	float lifetime;
	uint index;
	uint number;
	float age;
	float normalized_age;
	float normalized_index;
	float normalized_number;
	vec3 initial_position;
	vec3 initial_scale;
	vec4 initial_color;
	vec3 initial_velocity;
	float radial_velocity;
	vec3 orbital_axis;
	float initial_orbital_speed;
	vec3 spawn_direction;
	vec3 position;
	vec3 scale;
	vec4 color;
	vec3 velocity;
	vec3 orbital_velocity;
	float orbital_speed;
	vec3 rotation_axis;
	float initial_rotation;
	float rotation;
	mat3 orientation;
	vec3 force;
	float animation_frame_normalized;
	mat4 emission_transform;
	mat4 previous_transform;
	bool just_spawned;
	bool collided;
	vec3 collision_normal;
	float collision_depth;
};

float remap(float value, float input_min, float input_max, float output_min, float output_max) {
	float v = clamp(value, input_min, input_max);
	return (v - input_min) / (input_max - input_min) * (output_max - output_min) + output_min;
}

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}

uint hash(uint x) {
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = (x >> uint(16)) ^ x;
	return x;
}

vec3 normalize_or_zero(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 0.0, 0.0);
}

vec3 normalize_or_up(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 1.0, 0.0);
}

vec4 quat(vec3 axis, float angle) {
  float s = sin(angle / 2.0);
  return vec4(axis.x * s, axis.y * s, axis.z * s, cos(angle / 2.0));
}

vec4 quat_rotation_to(vec3 from, vec3 to) {
	vec3 xunit = vec3(1.0, 0.0, 0.0);
	vec3 yunit = vec3(0.0, 1.0, 0.0);
	float dot = dot(from, to);
	if (dot < -0.999999) {
	    vec3 axis = cross(xunit, from);
	    if (length(axis) < 0.000001) {
	    	axis = cross(yunit, from);
	    }
	    axis = normalize(axis);
	    return quat(axis, PI);
	} else if (dot > 0.999999) {
		return vec4(0.0, 0.0, 0.0, 1.0);
	} else {
		vec3 axis = cross(from, to);
		return normalize(vec4(axis, 1.0 + dot));
	}
}

vec3 rotated(vec3 v, vec3 axis, float angle) {
  vec4 q = quat(axis, angle);
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

vec3 rotated(vec3 v, vec4 q) {
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

mat3 rotated(mat3 m, vec4 q) {
  return mat3(rotated(m[0], q), rotated(m[1], q), rotated(m[2], q));
}

float normalized_by_amount(uint n) {
	return float(n) / float(max(AMOUNT, 1u));
}
void update_orbital_velocity(inout Particle particle, mat4 emission_transform, mat4 transform) {
	if (abs(particle.orbital_speed) < 0.01 || particle.delta < 0.001) { return; }
	vec3 local_pos = (inverse(emission_transform) * transform[3]).xyz;
	vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
	particle.position = (emission_transform * vec4(next, 0.0)).xyz;
	particle.orbital_velocity = (next - local_pos) / particle.delta;
}

void apply_radial_velocity(inout Particle particle) {
	vec3 d = particle.position;
	float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
	particle.position += normalize_or_zero(d) * mag * sign(particle.radial_velocity);
}

void m0_shape_location_sphere(inout Particle particle) {
	float r = rand_range(particle.seed, 0.000000, 1.000000);
	float u = rand_range(particle.seed, 0.000000, 1.000000);
	float v = rand_range(particle.seed, 0.000000, 1.000000);
	vec3 n = vec3(1.0, 0.0, 0.0);
	n = rotated(n, vec3(0.0, 1.0, 0.0), u * 360.0 * DEG_TO_RAD);
	float h = 1.0 - 2.0 * v;
	n = normalize(n * (1.0 - abs(h)) + vec3(0.0, h, 0.0));
	particle.spawn_direction = normalize_or_up(n * vec3(1.000000, 1.000000, 1.000000));
	particle.initial_position = particle.spawn_direction * r * 0.010000 + vec3(0.000000, 0.000000, 0.000000);
}

void m1_initialize_velocity(inout Particle particle) {
	float v = rand_range(particle.seed, 0.100000, 0.500000);
	particle.initial_velocity = particle.spawn_direction * v;
}

void m2_initialize_rotation(inout Particle particle) {
	float a = rand_range(particle.seed, 0.000000, 360.000000);
	particle.rotation_axis = vec3(0.000000, 0.000000, 1.000000);
	particle.initial_rotation = a * DEG_TO_RAD;
}

void m3_initialize_size(inout Particle particle) {
	float s_c = rand_range(particle.seed, 1.250000, 1.750000);
	vec3 s = vec3(s_c, s_c, s_c);
	particle.initial_scale = s;
}

uniform sampler2D u_m4_alpha : repeat_disable;

void m4_scale_color(inout Particle particle) {
	vec4 c = vec4(1.000000, 1.000000, 1.000000, 1.000000);
	float a = texture(u_m4_alpha, vec2(particle.normalized_age)).r;
	particle.color = particle.initial_color * vec4(c.rgb, a);
}

void m5_drag(inout Particle particle) {
	float limit = 0.0;
	float drag = 5.0;
	float speed = length(particle.velocity);
	float excess = max(0.0, speed - limit);
	vec3 dir = normalize_or_up(particle.velocity);
	particle.velocity = dir * (speed - particle.delta * excess * max(drag, 0.0));
}

uniform sampler2D u_m6_scale : repeat_disable;

void m6_scale_size(inout Particle particle) {
	vec3 s = vec3(texture(u_m6_scale, vec2(particle.normalized_age)).r);
	particle.scale *= s;
}

void initialize(inout Particle particle) {
	particle.initial_position = vec3(0.0);
	particle.initial_color = vec4(1.0);
	particle.initial_scale = vec3(1.0);
	particle.initial_velocity = vec3(0.0);
	particle.initial_orbital_speed = 0.0;
	particle.spawn_direction = vec3(0.0);
	particle.animation_frame_normalized = 0.0;
	particle.rotation_axis = vec3(1.0, 0.0, 0.0);
	particle.initial_rotation = 0.0;
	particle.radial_velocity = 0.0;
	particle.orbital_axis = vec3(0.0, 1.0, 0.0);
	particle.orbital_velocity = vec3(0.0);
	m0_shape_location_sphere(particle);
	m1_initialize_velocity(particle);
	m2_initialize_rotation(particle);
	m3_initialize_size(particle);
	particle.scale = particle.initial_scale;
	particle.color = particle.initial_color;
	particle.orbital_speed = particle.initial_orbital_speed;
}

void start() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = vec3(0.0);
	initialize(particle);
	if (rand(particle.seed) > AMOUNT_RATIO) {
		ACTIVE = false;
	}
	if (RESTART_CUSTOM) {
		CUSTOM = vec4(0.0);
		CUSTOM.x = particle.initial_rotation;
	}
	if (RESTART_COLOR) {
		COLOR = particle.initial_color;
	}
	if (RESTART_ROT_SCALE) {
		TRANSFORM[0].xyz = rotated(vec3(1.0, 0.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.x;
		TRANSFORM[1].xyz = rotated(vec3(0.0, 1.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.y;
		TRANSFORM[2].xyz = rotated(vec3(0.0, 0.0, 1.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.z;
	}
	if (RESTART_POSITION) {
		TRANSFORM[3].xyz = particle.initial_position;
		TRANSFORM = EMISSION_TRANSFORM * TRANSFORM;
	}
	if (RESTART_VELOCITY) {
		VELOCITY = (EMISSION_TRANSFORM * vec4(particle.initial_velocity, 0.0)).xyz;
	}
}

void process() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.delta = DELTA;
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = TRANSFORM[3].xyz;
	particle.velocity = VELOCITY;
	particle.orientation = mat3(
		normalize(TRANSFORM[0].xyz),
		normalize(TRANSFORM[1].xyz),
		normalize(TRANSFORM[2].xyz));
	particle.force = vec3(0.0);
	particle.emission_transform = EMISSION_TRANSFORM;
	particle.previous_transform = TRANSFORM;
	particle.rotation = CUSTOM.x;
	particle.just_spawned = CUSTOM.y == 0.0;
	particle.collided = COLLIDED;
	particle.collision_normal = COLLISION_NORMAL;
	particle.collision_depth = COLLISION_DEPTH;
	particle.color = COLOR;
	initialize(particle);
	CUSTOM.y += DELTA / particle.lifetime;
	CUSTOM.y = mix(CUSTOM.y, 1.0, INTERPOLATE_TO_END);
	CUSTOM.y = clamp(CUSTOM.y, 0.0, 1.0);
	particle.age = CUSTOM.y * particle.lifetime;
	particle.normalized_age = CUSTOM.y;
	m4_scale_color(particle);
	m5_drag(particle);
	m6_scale_size(particle);
	if (abs(particle.orbital_speed) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
		particle.orbital_velocity = (next - local_pos) / particle.delta;
	}
	if (abs(particle.radial_velocity) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 d = local_pos;
		float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
		vec3 next = local_pos + normalize_or_zero(d) * mag * sign(particle.radial_velocity);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
	}
	particle.position += particle.velocity * particle.delta;
	VELOCITY = particle.velocity;
	TRANSFORM[0].xyz = particle.orientation[0] * particle.scale.x;
	TRANSFORM[1].xyz = particle.orientation[1] * particle.scale.y;
	TRANSFORM[2].xyz = particle.orientation[2] * particle.scale.z;
	TRANSFORM[3].xyz = particle.position;
	COLOR = particle.color;
	CUSTOM.x = particle.rotation;
	
	if (particle.age >= LIFETIME) { ACTIVE = false;  }
}

"

[sub_resource type="Curve" id="Curve_f1m5s"]
_data = [Vector2(0, 0), 0.0, 3.4292428, 0, 0, Vector2(0.30333334, 1), 0.0, 0.0, 0, 0]
point_count = 2

[sub_resource type="CurveTexture" id="CurveTexture_kvtc6"]
curve = SubResource("Curve_f1m5s")
metadata/uniform_name = "u_m4_alpha"

[sub_resource type="Curve" id="Curve_edoys"]
_data = [Vector2(0, 0), 0.0, 2.2327468, 0, 0, Vector2(1, 1), 0.0, 0.0, 0, 0]
point_count = 2

[sub_resource type="CurveTexture" id="CurveTexture_7yqc4"]
curve = SubResource("Curve_edoys")
metadata/uniform_name = "u_m6_scale"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_unohw"]
shader = SubResource("Shader_ryygs")
shader_parameter/u_m4_alpha = SubResource("CurveTexture_kvtc6")
shader_parameter/u_m6_scale = SubResource("CurveTexture_7yqc4")

[sub_resource type="Shader" id="Shader_h4xed"]
code = "shader_type spatial;
render_mode unshaded, cull_disabled;

uniform sampler2D mask_tex : source_color, filter_linear_mipmap, repeat_disable;

uniform sampler2D noise1_tex : source_color, filter_linear_mipmap, repeat_enable;
uniform vec2 noise1_tiling = vec2(1.0);
uniform vec2 noise1_offset = vec2(0.0);
uniform vec2 noise1_speed = vec2(0.0);

uniform sampler2D noise2_tex : source_color, filter_linear_mipmap, repeat_enable;
uniform vec2 noise2_tiling = vec2(1.0);
uniform vec2 noise2_offset = vec2(0.0);
uniform vec2 noise2_speed = vec2(0.0);

uniform sampler2D gradient_tex : source_color, filter_linear_mipmap, repeat_enable;
uniform vec2 gradient_tiling = vec2(1.0);
uniform vec2 gradient_offset = vec2(0.0);
uniform vec2 gradient_speed = vec2(0.0);

uniform sampler2D gradient : source_color, repeat_disable;
uniform sampler2D emission_curve : repeat_disable;

uniform float dissolve_feather = 0.025;
uniform sampler2D dissolve_curve : filter_linear, repeat_disable;

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}

float remap(float value, float input_min, float input_max, float output_min, float output_max) {
	return (clamp(value, input_min, input_max) - input_min) / (input_max - input_min) * (output_max - output_min) + output_min;
}

varying flat uint instance_id;
varying float age;

void vertex()
{
	instance_id = uint(INSTANCE_ID);
	age = INSTANCE_CUSTOM.y;
	vec4 offset = vec4(0.0);
	mat4 model = mat4(
		normalize(MAIN_CAM_INV_VIEW_MATRIX[0]),
		normalize(MAIN_CAM_INV_VIEW_MATRIX[1]),
		normalize(MAIN_CAM_INV_VIEW_MATRIX[2]),
		MODEL_MATRIX[3] + offset);
	model = model * mat4(
		vec4(cos(INSTANCE_CUSTOM.x), -sin(INSTANCE_CUSTOM.x), 0.0, 0.0),
		vec4(sin(INSTANCE_CUSTOM.x), cos(INSTANCE_CUSTOM.x), 0.0, 0.0),
		vec4(0.0, 0.0, 1.0, 0.0),
		vec4(0.0, 0.0, 0.0, 1.0));
	model = model * mat4(
		vec4(length(MODEL_MATRIX[0].xyz), 0.0, 0.0, 0.0),
		vec4(0.0, length(MODEL_MATRIX[1].xyz), 0.0, 0.0),
		vec4(0.0, 0.0, length(MODEL_MATRIX[2].xyz), 0.0),
		vec4(0.0, 0.0, 0.0, 1.0));
	MODELVIEW_MATRIX = VIEW_MATRIX * model;
	MODELVIEW_NORMAL_MATRIX = mat3(MODELVIEW_MATRIX);
}

void fragment()
{
	uint seed = instance_id;
	vec4 mask_s = texture(mask_tex, UV);
	vec4 noise1_s = texture(noise1_tex, UV * noise1_tiling + noise1_offset + vec2(rand_range(seed, 0.0, 1.0), rand_range(seed, 0.0, 1.0)) + noise1_speed * TIME);
	vec4 noise2_s = texture(noise2_tex, UV * noise2_tiling + noise2_offset + vec2(rand_range(seed, 0.0, 1.0), rand_range(seed, 0.0, 1.0)) + noise2_speed * TIME);
	vec4 gradient_s = texture(gradient_tex, UV * gradient_tiling + gradient_offset + vec2(rand_range(seed, 0.0, 1.0), rand_range(seed, 0.0, 1.0)) + gradient_speed * TIME);
	float dissolve_t = texture(dissolve_curve, vec2(age)).r;
	dissolve_t = remap(dissolve_t, 0.0, 1.0, -dissolve_feather, 1.0);
	ALPHA = COLOR.a;
	ALPHA *= mask_s.r;
	ALPHA *= noise1_s.r;
	ALPHA *= noise2_s.r;
	ALPHA = clamp(ALPHA, 0.0, 1.0);
	ALPHA *= smoothstep(dissolve_t, dissolve_t + dissolve_feather, ALPHA);
	ALBEDO = COLOR.rgb;
	ALBEDO *= texture(gradient, vec2(gradient_s.r)).rgb;
	ALBEDO *= texture(emission_curve, vec2(ALPHA)).r;
}"

[sub_resource type="Curve" id="Curve_ksnwm"]
_data = [Vector2(0, 0), 0.0, 1.0, 0, 1, Vector2(1, 1), 1.0, 0.0, 1, 0]
point_count = 2

[sub_resource type="CurveTexture" id="CurveTexture_vbv8k"]
curve = SubResource("Curve_ksnwm")

[sub_resource type="Curve" id="Curve_ex7wl"]
_limits = [1.0, 20.0, 0.0, 1.0]
_data = [Vector2(0, 1), 0.0, 0.0, 0, 0, Vector2(1, 20), 36.45624, 0.0, 0, 0]
point_count = 2

[sub_resource type="CurveTexture" id="CurveTexture_lfv8q"]
curve = SubResource("Curve_ex7wl")

[sub_resource type="ShaderMaterial" id="ShaderMaterial_6g0k6"]
render_priority = -2
shader = SubResource("Shader_h4xed")
shader_parameter/mask_tex = ExtResource("2_582q2")
shader_parameter/noise1_tex = ExtResource("1_wk6d5")
shader_parameter/noise1_tiling = Vector2(0.5, 0.5)
shader_parameter/noise1_offset = Vector2(0, 0)
shader_parameter/noise1_speed = Vector2(-0.07, 0.35)
shader_parameter/noise2_tex = ExtResource("3_i1ty8")
shader_parameter/noise2_tiling = Vector2(0.2, 0.2)
shader_parameter/noise2_offset = Vector2(0, 0)
shader_parameter/noise2_speed = Vector2(0.055, 0.145)
shader_parameter/gradient_tex = ExtResource("1_wk6d5")
shader_parameter/gradient_tiling = Vector2(0.25, 0.25)
shader_parameter/gradient_offset = Vector2(0, 0)
shader_parameter/gradient_speed = Vector2(0.045, 0.2)
shader_parameter/gradient = SubResource("GradientTexture1D_k04dp")
shader_parameter/emission_curve = SubResource("CurveTexture_lfv8q")
shader_parameter/dissolve_feather = 0.5
shader_parameter/dissolve_curve = SubResource("CurveTexture_vbv8k")

[sub_resource type="QuadMesh" id="QuadMesh_vmox8"]
material = SubResource("ShaderMaterial_6g0k6")

[sub_resource type="Shader" id="Shader_3ebla"]
code = "shader_type particles;
render_mode disable_velocity;

const float DEG_TO_RAD = PI / 180.0;
const uint AMOUNT = uint(1);

struct Particle {
	uint seed;
	float delta;
	float lifetime;
	uint index;
	uint number;
	float age;
	float normalized_age;
	float normalized_index;
	float normalized_number;
	vec3 initial_position;
	vec3 initial_scale;
	vec4 initial_color;
	vec3 initial_velocity;
	float radial_velocity;
	vec3 orbital_axis;
	float initial_orbital_speed;
	vec3 spawn_direction;
	vec3 position;
	vec3 scale;
	vec4 color;
	vec3 velocity;
	vec3 orbital_velocity;
	float orbital_speed;
	vec3 rotation_axis;
	float initial_rotation;
	float rotation;
	mat3 orientation;
	vec3 force;
	float animation_frame_normalized;
	mat4 emission_transform;
	mat4 previous_transform;
	bool just_spawned;
	bool collided;
	vec3 collision_normal;
	float collision_depth;
};

float remap(float value, float input_min, float input_max, float output_min, float output_max) {
	float v = clamp(value, input_min, input_max);
	return (v - input_min) / (input_max - input_min) * (output_max - output_min) + output_min;
}

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}

uint hash(uint x) {
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = (x >> uint(16)) ^ x;
	return x;
}

vec3 normalize_or_zero(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 0.0, 0.0);
}

vec3 normalize_or_up(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 1.0, 0.0);
}

vec4 quat(vec3 axis, float angle) {
  float s = sin(angle / 2.0);
  return vec4(axis.x * s, axis.y * s, axis.z * s, cos(angle / 2.0));
}

vec4 quat_rotation_to(vec3 from, vec3 to) {
	vec3 xunit = vec3(1.0, 0.0, 0.0);
	vec3 yunit = vec3(0.0, 1.0, 0.0);
	float dot = dot(from, to);
	if (dot < -0.999999) {
	    vec3 axis = cross(xunit, from);
	    if (length(axis) < 0.000001) {
	    	axis = cross(yunit, from);
	    }
	    axis = normalize(axis);
	    return quat(axis, PI);
	} else if (dot > 0.999999) {
		return vec4(0.0, 0.0, 0.0, 1.0);
	} else {
		vec3 axis = cross(from, to);
		return normalize(vec4(axis, 1.0 + dot));
	}
}

vec3 rotated(vec3 v, vec3 axis, float angle) {
  vec4 q = quat(axis, angle);
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

vec3 rotated(vec3 v, vec4 q) {
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

mat3 rotated(mat3 m, vec4 q) {
  return mat3(rotated(m[0], q), rotated(m[1], q), rotated(m[2], q));
}

float normalized_by_amount(uint n) {
	return float(n) / float(max(AMOUNT, 1u));
}
void update_orbital_velocity(inout Particle particle, mat4 emission_transform, mat4 transform) {
	if (abs(particle.orbital_speed) < 0.01 || particle.delta < 0.001) { return; }
	vec3 local_pos = (inverse(emission_transform) * transform[3]).xyz;
	vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
	particle.position = (emission_transform * vec4(next, 0.0)).xyz;
	particle.orbital_velocity = (next - local_pos) / particle.delta;
}

void apply_radial_velocity(inout Particle particle) {
	vec3 d = particle.position;
	float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
	particle.position += normalize_or_zero(d) * mag * sign(particle.radial_velocity);
}

uniform vec4 initial_color : source_color;

void m0_initialize_color(inout Particle particle) {
	vec4 c = initial_color;
	float a = 0.2;
	particle.initial_color = vec4(c.rgb, a);
}

void m1_initialize_size(inout Particle particle) {
	vec3 s = vec3(1.500000, 1.500000, 1.500000);
	particle.initial_scale = s;
}

void initialize(inout Particle particle) {
	particle.initial_position = vec3(0.0);
	particle.initial_color = vec4(1.0);
	particle.initial_scale = vec3(1.0);
	particle.initial_velocity = vec3(0.0);
	particle.initial_orbital_speed = 0.0;
	particle.spawn_direction = vec3(0.0);
	particle.animation_frame_normalized = 0.0;
	particle.rotation_axis = vec3(1.0, 0.0, 0.0);
	particle.initial_rotation = 0.0;
	particle.radial_velocity = 0.0;
	particle.orbital_axis = vec3(0.0, 1.0, 0.0);
	particle.orbital_velocity = vec3(0.0);
	m0_initialize_color(particle);
	m1_initialize_size(particle);
	particle.scale = particle.initial_scale;
	particle.color = particle.initial_color;
	particle.orbital_speed = particle.initial_orbital_speed;
}

void start() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = vec3(0.0);
	initialize(particle);
	if (rand(particle.seed) > AMOUNT_RATIO) {
		ACTIVE = false;
	}
	if (RESTART_CUSTOM) {
		CUSTOM = vec4(0.0);
		CUSTOM.x = particle.initial_rotation;
	}
	if (RESTART_COLOR) {
		COLOR = particle.initial_color;
	}
	if (RESTART_ROT_SCALE) {
		TRANSFORM[0].xyz = rotated(vec3(1.0, 0.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.x;
		TRANSFORM[1].xyz = rotated(vec3(0.0, 1.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.y;
		TRANSFORM[2].xyz = rotated(vec3(0.0, 0.0, 1.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.z;
	}
	if (RESTART_POSITION) {
		TRANSFORM[3].xyz = particle.initial_position;
		TRANSFORM = EMISSION_TRANSFORM * TRANSFORM;
	}
	if (RESTART_VELOCITY) {
		VELOCITY = (EMISSION_TRANSFORM * vec4(particle.initial_velocity, 0.0)).xyz;
	}
}

void process() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.delta = DELTA;
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = TRANSFORM[3].xyz;
	particle.velocity = VELOCITY;
	particle.orientation = mat3(
		normalize(TRANSFORM[0].xyz),
		normalize(TRANSFORM[1].xyz),
		normalize(TRANSFORM[2].xyz));
	particle.force = vec3(0.0);
	particle.emission_transform = EMISSION_TRANSFORM;
	particle.previous_transform = TRANSFORM;
	particle.rotation = CUSTOM.x;
	particle.just_spawned = CUSTOM.y == 0.0;
	particle.collided = COLLIDED;
	particle.collision_normal = COLLISION_NORMAL;
	particle.collision_depth = COLLISION_DEPTH;
	particle.color = COLOR;
	initialize(particle);
	CUSTOM.y += DELTA / particle.lifetime;
	CUSTOM.y = mix(CUSTOM.y, 1.0, INTERPOLATE_TO_END);
	CUSTOM.y = clamp(CUSTOM.y, 0.0, 1.0);
	particle.age = CUSTOM.y * particle.lifetime;
	particle.normalized_age = CUSTOM.y;
	if (abs(particle.orbital_speed) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
		particle.orbital_velocity = (next - local_pos) / particle.delta;
	}
	if (abs(particle.radial_velocity) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 d = local_pos;
		float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
		vec3 next = local_pos + normalize_or_zero(d) * mag * sign(particle.radial_velocity);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
	}
	particle.position += particle.velocity * particle.delta;
	VELOCITY = particle.velocity;
	TRANSFORM[0].xyz = particle.orientation[0] * particle.scale.x;
	TRANSFORM[1].xyz = particle.orientation[1] * particle.scale.y;
	TRANSFORM[2].xyz = particle.orientation[2] * particle.scale.z;
	TRANSFORM[3].xyz = particle.position;
	COLOR = particle.color;
	CUSTOM.x = particle.rotation;
	
	if (particle.age >= LIFETIME) { ACTIVE = false;  }
}

"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_c1r1o"]
shader = SubResource("Shader_3ebla")
shader_parameter/initial_color = Color(0.96, 0.60144, 0.1632, 1)

[sub_resource type="Shader" id="Shader_p0im6"]
code = "shader_type spatial;
render_mode unshaded, blend_add, cull_disabled;

uniform float opacity = 1.0;
uniform float emission = 1.0;

uniform sampler2D main_tex : source_color, filter_linear_mipmap, repeat_disable;

varying flat uint instance_id;
varying float age;

void vertex()
{
	instance_id = uint(INSTANCE_ID);
	age = INSTANCE_CUSTOM.y;
	vec4 offset = vec4(0.0);
	mat4 model = mat4(
		normalize(MAIN_CAM_INV_VIEW_MATRIX[0]),
		normalize(MAIN_CAM_INV_VIEW_MATRIX[1]),
		normalize(MAIN_CAM_INV_VIEW_MATRIX[2]),
		MODEL_MATRIX[3] + offset);
	model = model * mat4(
		vec4(cos(INSTANCE_CUSTOM.x), -sin(INSTANCE_CUSTOM.x), 0.0, 0.0),
		vec4(sin(INSTANCE_CUSTOM.x), cos(INSTANCE_CUSTOM.x), 0.0, 0.0),
		vec4(0.0, 0.0, 1.0, 0.0),
		vec4(0.0, 0.0, 0.0, 1.0));
	model = model * mat4(
		vec4(length(MODEL_MATRIX[0].xyz), 0.0, 0.0, 0.0),
		vec4(0.0, length(MODEL_MATRIX[1].xyz), 0.0, 0.0),
		vec4(0.0, 0.0, length(MODEL_MATRIX[2].xyz), 0.0),
		vec4(0.0, 0.0, 0.0, 1.0));
	MODELVIEW_MATRIX = VIEW_MATRIX * model;
	MODELVIEW_NORMAL_MATRIX = mat3(MODELVIEW_MATRIX);
}

void fragment()
{
	vec4 main_s = texture(main_tex, UV);
	ALPHA = COLOR.a;
	ALPHA *= main_s.r;
	ALPHA = clamp(ALPHA * opacity, 0.0, 1.0);
	ALBEDO = COLOR.rgb * emission;
	ALBEDO *= main_s.rgb;
	
}


"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_yj1h7"]
render_priority = -3
shader = SubResource("Shader_p0im6")
shader_parameter/opacity = 0.46
shader_parameter/emission = 1.0
shader_parameter/main_tex = ExtResource("4_gm0l3")

[sub_resource type="QuadMesh" id="QuadMesh_rw7bw"]
material = SubResource("ShaderMaterial_yj1h7")

[sub_resource type="Shader" id="Shader_uv1v1"]
code = "shader_type particles;
render_mode disable_velocity;

const float DEG_TO_RAD = PI / 180.0;
const uint AMOUNT = uint(6);

struct Particle {
	uint seed;
	float delta;
	float lifetime;
	uint index;
	uint number;
	float age;
	float normalized_age;
	float normalized_index;
	float normalized_number;
	vec3 initial_position;
	vec3 initial_scale;
	vec4 initial_color;
	vec3 initial_velocity;
	float radial_velocity;
	vec3 orbital_axis;
	float initial_orbital_speed;
	vec3 spawn_direction;
	vec3 position;
	vec3 scale;
	vec4 color;
	vec3 velocity;
	vec3 orbital_velocity;
	float orbital_speed;
	vec3 rotation_axis;
	float initial_rotation;
	float rotation;
	mat3 orientation;
	vec3 force;
	float animation_frame_normalized;
	mat4 emission_transform;
	mat4 previous_transform;
	bool just_spawned;
	bool collided;
	vec3 collision_normal;
	float collision_depth;
};

float remap(float value, float input_min, float input_max, float output_min, float output_max) {
	float v = clamp(value, input_min, input_max);
	return (v - input_min) / (input_max - input_min) * (output_max - output_min) + output_min;
}

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}

uint hash(uint x) {
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = (x >> uint(16)) ^ x;
	return x;
}

vec3 normalize_or_zero(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 0.0, 0.0);
}

vec3 normalize_or_up(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 1.0, 0.0);
}

vec4 quat(vec3 axis, float angle) {
  float s = sin(angle / 2.0);
  return vec4(axis.x * s, axis.y * s, axis.z * s, cos(angle / 2.0));
}

vec4 quat_rotation_to(vec3 from, vec3 to) {
	vec3 xunit = vec3(1.0, 0.0, 0.0);
	vec3 yunit = vec3(0.0, 1.0, 0.0);
	float dot = dot(from, to);
	if (dot < -0.999999) {
	    vec3 axis = cross(xunit, from);
	    if (length(axis) < 0.000001) {
	    	axis = cross(yunit, from);
	    }
	    axis = normalize(axis);
	    return quat(axis, PI);
	} else if (dot > 0.999999) {
		return vec4(0.0, 0.0, 0.0, 1.0);
	} else {
		vec3 axis = cross(from, to);
		return normalize(vec4(axis, 1.0 + dot));
	}
}

vec3 rotated(vec3 v, vec3 axis, float angle) {
  vec4 q = quat(axis, angle);
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

vec3 rotated(vec3 v, vec4 q) {
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

mat3 rotated(mat3 m, vec4 q) {
  return mat3(rotated(m[0], q), rotated(m[1], q), rotated(m[2], q));
}

float normalized_by_amount(uint n) {
	return float(n) / float(max(AMOUNT, 1u));
}
void update_orbital_velocity(inout Particle particle, mat4 emission_transform, mat4 transform) {
	if (abs(particle.orbital_speed) < 0.01 || particle.delta < 0.001) { return; }
	vec3 local_pos = (inverse(emission_transform) * transform[3]).xyz;
	vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
	particle.position = (emission_transform * vec4(next, 0.0)).xyz;
	particle.orbital_velocity = (next - local_pos) / particle.delta;
}

void apply_radial_velocity(inout Particle particle) {
	vec3 d = particle.position;
	float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
	particle.position += normalize_or_zero(d) * mag * sign(particle.radial_velocity);
}

void m0_initialize_size(inout Particle particle) {
	float s_c = rand_range(particle.seed, 0.020000, 0.030000);
	vec3 s = vec3(s_c, s_c, s_c);
	particle.initial_scale = s;
}

void m1_shape_location_sphere(inout Particle particle) {
	float r = rand_range(particle.seed, 0.000000, 1.000000);
	float u = rand_range(particle.seed, 0.000000, 1.000000);
	float v = rand_range(particle.seed, 0.000000, 1.000000);
	vec3 n = vec3(1.0, 0.0, 0.0);
	n = rotated(n, vec3(0.0, 1.0, 0.0), u * 360.0 * DEG_TO_RAD);
	float h = 1.0 - 2.0 * v;
	n = normalize(n * (1.0 - abs(h)) + vec3(0.0, h, 0.0));
	particle.spawn_direction = normalize_or_up(n * vec3(1.000000, 1.000000, 1.000000));
	particle.initial_position = particle.spawn_direction * r * 0.400000 + vec3(0.000000, 0.000000, 0.000000);
}

void m2_initialize_velocity(inout Particle particle) {
	float v = rand_range(particle.seed, 0.010000, 0.250000);
	particle.initial_velocity = particle.spawn_direction * v;
}

uniform vec4 initial_color : source_color;

void m3_initialize_color(inout Particle particle) {
	vec4 c = initial_color;
	float a = 1.0;
	particle.initial_color = vec4(c.rgb, a);
}

void m4_initialize_velocity(inout Particle particle) {
	float v = rand_range(particle.seed, -3.000000, 3.000000);
	particle.orbital_axis = vec3(0.000000, 1.000000, 0.000000);
	particle.initial_orbital_speed = v;
}

uniform sampler2D u_m5_scale : repeat_disable;

void m5_scale_size(inout Particle particle) {
	vec3 s = vec3(texture(u_m5_scale, vec2(particle.normalized_age)).r);
	particle.scale *= s;
}

void m6_drag(inout Particle particle) {
	float limit = 0.25;
	float drag = 10.0;
	float speed = length(particle.velocity);
	float excess = max(0.0, speed - limit);
	vec3 dir = normalize_or_up(particle.velocity);
	particle.velocity = dir * (speed - particle.delta * excess * max(drag, 0.0));
}

void initialize(inout Particle particle) {
	particle.initial_position = vec3(0.0);
	particle.initial_color = vec4(1.0);
	particle.initial_scale = vec3(1.0);
	particle.initial_velocity = vec3(0.0);
	particle.initial_orbital_speed = 0.0;
	particle.spawn_direction = vec3(0.0);
	particle.animation_frame_normalized = 0.0;
	particle.rotation_axis = vec3(1.0, 0.0, 0.0);
	particle.initial_rotation = 0.0;
	particle.radial_velocity = 0.0;
	particle.orbital_axis = vec3(0.0, 1.0, 0.0);
	particle.orbital_velocity = vec3(0.0);
	m0_initialize_size(particle);
	m1_shape_location_sphere(particle);
	m2_initialize_velocity(particle);
	m3_initialize_color(particle);
	m4_initialize_velocity(particle);
	particle.scale = particle.initial_scale;
	particle.color = particle.initial_color;
	particle.orbital_speed = particle.initial_orbital_speed;
}

void start() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = vec3(0.0);
	initialize(particle);
	if (rand(particle.seed) > AMOUNT_RATIO) {
		ACTIVE = false;
	}
	if (RESTART_CUSTOM) {
		CUSTOM = vec4(0.0);
		CUSTOM.x = particle.initial_rotation;
	}
	if (RESTART_COLOR) {
		COLOR = particle.initial_color;
	}
	if (RESTART_ROT_SCALE) {
		TRANSFORM[0].xyz = rotated(vec3(1.0, 0.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.x;
		TRANSFORM[1].xyz = rotated(vec3(0.0, 1.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.y;
		TRANSFORM[2].xyz = rotated(vec3(0.0, 0.0, 1.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.z;
	}
	if (RESTART_POSITION) {
		TRANSFORM[3].xyz = particle.initial_position;
		TRANSFORM = EMISSION_TRANSFORM * TRANSFORM;
	}
	if (RESTART_VELOCITY) {
		VELOCITY = (EMISSION_TRANSFORM * vec4(particle.initial_velocity, 0.0)).xyz;
	}
}

void process() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.delta = DELTA;
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = TRANSFORM[3].xyz;
	particle.velocity = VELOCITY;
	particle.orientation = mat3(
		normalize(TRANSFORM[0].xyz),
		normalize(TRANSFORM[1].xyz),
		normalize(TRANSFORM[2].xyz));
	particle.force = vec3(0.0);
	particle.emission_transform = EMISSION_TRANSFORM;
	particle.previous_transform = TRANSFORM;
	particle.rotation = CUSTOM.x;
	particle.just_spawned = CUSTOM.y == 0.0;
	particle.collided = COLLIDED;
	particle.collision_normal = COLLISION_NORMAL;
	particle.collision_depth = COLLISION_DEPTH;
	particle.color = COLOR;
	initialize(particle);
	CUSTOM.y += DELTA / particle.lifetime;
	CUSTOM.y = mix(CUSTOM.y, 1.0, INTERPOLATE_TO_END);
	CUSTOM.y = clamp(CUSTOM.y, 0.0, 1.0);
	particle.age = CUSTOM.y * particle.lifetime;
	particle.normalized_age = CUSTOM.y;
	m5_scale_size(particle);
	m6_drag(particle);
	if (abs(particle.orbital_speed) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
		particle.orbital_velocity = (next - local_pos) / particle.delta;
	}
	if (abs(particle.radial_velocity) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 d = local_pos;
		float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
		vec3 next = local_pos + normalize_or_zero(d) * mag * sign(particle.radial_velocity);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
	}
	particle.position += particle.velocity * particle.delta;
	VELOCITY = particle.velocity;
	TRANSFORM[0].xyz = particle.orientation[0] * particle.scale.x;
	TRANSFORM[1].xyz = particle.orientation[1] * particle.scale.y;
	TRANSFORM[2].xyz = particle.orientation[2] * particle.scale.z;
	TRANSFORM[3].xyz = particle.position;
	COLOR = particle.color;
	CUSTOM.x = particle.rotation;
	
	if (particle.age >= LIFETIME) { ACTIVE = false;  }
}

"

[sub_resource type="Curve" id="Curve_1i44k"]
_data = [Vector2(0, 0), 0.0, 3.4731615, 0, 0, Vector2(0.26, 1), 0.0, 0.0, 0, 0, Vector2(1, 0), -1.9079846, 0.0, 0, 0]
point_count = 3

[sub_resource type="CurveTexture" id="CurveTexture_8aptq"]
curve = SubResource("Curve_1i44k")
metadata/uniform_name = "u_m5_scale"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_832lv"]
shader = SubResource("Shader_uv1v1")
shader_parameter/initial_color = Color(0.92, 0.7436667, 0.46, 1)
shader_parameter/u_m5_scale = SubResource("CurveTexture_8aptq")

[sub_resource type="Shader" id="Shader_o4c01"]
code = "shader_type spatial;
render_mode unshaded, cull_disabled;

uniform float opacity = 1.0;
uniform float emission = 1.0;

uniform sampler2D main_tex : source_color, filter_linear_mipmap, repeat_disable;

uniform float alpha_threshold : hint_range(0.0, 1.0, 0.01) = 0.01;
uniform float alpha_feather : hint_range(0.0, 1.0, 0.01) = 0.025;

varying flat uint instance_id;
varying float age;

void vertex()
{
	instance_id = uint(INSTANCE_ID);
	age = INSTANCE_CUSTOM.y;
	vec4 offset = vec4(0.0);
	mat4 model = mat4(
		normalize(MAIN_CAM_INV_VIEW_MATRIX[0]),
		normalize(MAIN_CAM_INV_VIEW_MATRIX[1]),
		normalize(MAIN_CAM_INV_VIEW_MATRIX[2]),
		MODEL_MATRIX[3] + offset);
	model = model * mat4(
		vec4(cos(INSTANCE_CUSTOM.x), -sin(INSTANCE_CUSTOM.x), 0.0, 0.0),
		vec4(sin(INSTANCE_CUSTOM.x), cos(INSTANCE_CUSTOM.x), 0.0, 0.0),
		vec4(0.0, 0.0, 1.0, 0.0),
		vec4(0.0, 0.0, 0.0, 1.0));
	model = model * mat4(
		vec4(length(MODEL_MATRIX[0].xyz), 0.0, 0.0, 0.0),
		vec4(0.0, length(MODEL_MATRIX[1].xyz), 0.0, 0.0),
		vec4(0.0, 0.0, length(MODEL_MATRIX[2].xyz), 0.0),
		vec4(0.0, 0.0, 0.0, 1.0));
	MODELVIEW_MATRIX = VIEW_MATRIX * model;
	MODELVIEW_NORMAL_MATRIX = mat3(MODELVIEW_MATRIX);
}

void fragment()
{
	vec4 main_s = texture(main_tex, UV);
	ALPHA = COLOR.a;
	ALPHA *= main_s.r;
	ALPHA = clamp(ALPHA * opacity, 0.0, 1.0);
	ALPHA = smoothstep(alpha_threshold, alpha_threshold + alpha_feather, ALPHA);
	ALBEDO = COLOR.rgb * emission;
	ALBEDO *= main_s.rgb;
	
}


"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_axkkq"]
render_priority = -1
shader = SubResource("Shader_o4c01")
shader_parameter/opacity = 1.0
shader_parameter/emission = 8.0
shader_parameter/main_tex = ExtResource("2_582q2")
shader_parameter/alpha_threshold = 0.16999999620016
shader_parameter/alpha_feather = 0.249999994412

[sub_resource type="QuadMesh" id="QuadMesh_fgan6"]
material = SubResource("ShaderMaterial_axkkq")

[sub_resource type="Shader" id="Shader_e5x8v"]
code = "shader_type particles;
render_mode disable_velocity;

const float DEG_TO_RAD = PI / 180.0;
const uint AMOUNT = uint(1);

struct Particle {
	uint seed;
	float delta;
	float lifetime;
	uint index;
	uint number;
	float age;
	float normalized_age;
	float normalized_index;
	float normalized_number;
	vec3 initial_position;
	vec3 initial_scale;
	vec4 initial_color;
	vec3 initial_velocity;
	float radial_velocity;
	vec3 orbital_axis;
	float initial_orbital_speed;
	vec3 spawn_direction;
	vec3 position;
	vec3 scale;
	vec4 color;
	vec3 velocity;
	vec3 orbital_velocity;
	float orbital_speed;
	vec3 rotation_axis;
	float initial_rotation;
	float rotation;
	mat3 orientation;
	vec3 force;
	float animation_frame_normalized;
	mat4 emission_transform;
	mat4 previous_transform;
	bool just_spawned;
	bool collided;
	vec3 collision_normal;
	float collision_depth;
};

float remap(float value, float input_min, float input_max, float output_min, float output_max) {
	float v = clamp(value, input_min, input_max);
	return (v - input_min) / (input_max - input_min) * (output_max - output_min) + output_min;
}

float rand(inout uint seed) {
	int k;
	int s = int(seed);
	if (s == 0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}

float rand_range(inout uint seed, float l, float h) {
	return l + (h - l) * rand(seed);
}

uint hash(uint x) {
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = ((x >> uint(16)) ^ x) * uint(73244475);
	x = (x >> uint(16)) ^ x;
	return x;
}

vec3 normalize_or_zero(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 0.0, 0.0);
}

vec3 normalize_or_up(vec3 v) {
  return length(v) > 0.0 ? normalize(v) : vec3(0.0, 1.0, 0.0);
}

vec4 quat(vec3 axis, float angle) {
  float s = sin(angle / 2.0);
  return vec4(axis.x * s, axis.y * s, axis.z * s, cos(angle / 2.0));
}

vec4 quat_rotation_to(vec3 from, vec3 to) {
	vec3 xunit = vec3(1.0, 0.0, 0.0);
	vec3 yunit = vec3(0.0, 1.0, 0.0);
	float dot = dot(from, to);
	if (dot < -0.999999) {
	    vec3 axis = cross(xunit, from);
	    if (length(axis) < 0.000001) {
	    	axis = cross(yunit, from);
	    }
	    axis = normalize(axis);
	    return quat(axis, PI);
	} else if (dot > 0.999999) {
		return vec4(0.0, 0.0, 0.0, 1.0);
	} else {
		vec3 axis = cross(from, to);
		return normalize(vec4(axis, 1.0 + dot));
	}
}

vec3 rotated(vec3 v, vec3 axis, float angle) {
  vec4 q = quat(axis, angle);
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

vec3 rotated(vec3 v, vec4 q) {
  vec3 t = cross(q.xyz, v) + q.w * v;
  return v + 2.0 * cross(q.xyz, t);
}

mat3 rotated(mat3 m, vec4 q) {
  return mat3(rotated(m[0], q), rotated(m[1], q), rotated(m[2], q));
}

float normalized_by_amount(uint n) {
	return float(n) / float(max(AMOUNT, 1u));
}
void update_orbital_velocity(inout Particle particle, mat4 emission_transform, mat4 transform) {
	if (abs(particle.orbital_speed) < 0.01 || particle.delta < 0.001) { return; }
	vec3 local_pos = (inverse(emission_transform) * transform[3]).xyz;
	vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
	particle.position = (emission_transform * vec4(next, 0.0)).xyz;
	particle.orbital_velocity = (next - local_pos) / particle.delta;
}

void apply_radial_velocity(inout Particle particle) {
	vec3 d = particle.position;
	float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
	particle.position += normalize_or_zero(d) * mag * sign(particle.radial_velocity);
}

uniform vec4 initial_color : source_color;

void m0_initialize_color(inout Particle particle) {
	vec4 c = initial_color;
	float a = 1.0;
	particle.initial_color = vec4(c.rgb, a);
}

void m1_initialize_size(inout Particle particle) {
	vec3 s = vec3(2.000000, 2.000000, 2.000000);
	particle.initial_scale = s;
}

void m2_initialize_rotation(inout Particle particle) {
	float a = rand_range(particle.seed, 0.000000, 360.000000);
	particle.rotation_axis = vec3(0.000000, 0.000000, 1.000000);
	particle.initial_rotation = a * DEG_TO_RAD;
}

uniform sampler2D u_m3_scale : repeat_disable;

void m3_scale_size(inout Particle particle) {
	vec3 s = vec3(texture(u_m3_scale, vec2(particle.normalized_age)).r);
	particle.scale *= s;
}

uniform sampler2D u_m4_alpha : repeat_disable;

void m4_scale_color(inout Particle particle) {
	vec4 c = vec4(1.000000, 1.000000, 1.000000, 1.000000);
	float a = texture(u_m4_alpha, vec2(particle.normalized_age)).r;
	particle.color = particle.initial_color * vec4(c.rgb, a);
}

void m5_add_rotation(inout Particle particle) {
	float a = 10.0;
	particle.rotation += a * DEG_TO_RAD * particle.delta;
	particle.orientation = rotated(mat3(1.0), quat(particle.rotation_axis, particle.rotation));
}

void initialize(inout Particle particle) {
	particle.initial_position = vec3(0.0);
	particle.initial_color = vec4(1.0);
	particle.initial_scale = vec3(1.0);
	particle.initial_velocity = vec3(0.0);
	particle.initial_orbital_speed = 0.0;
	particle.spawn_direction = vec3(0.0);
	particle.animation_frame_normalized = 0.0;
	particle.rotation_axis = vec3(1.0, 0.0, 0.0);
	particle.initial_rotation = 0.0;
	particle.radial_velocity = 0.0;
	particle.orbital_axis = vec3(0.0, 1.0, 0.0);
	particle.orbital_velocity = vec3(0.0);
	m0_initialize_color(particle);
	m1_initialize_size(particle);
	m2_initialize_rotation(particle);
	particle.scale = particle.initial_scale;
	particle.color = particle.initial_color;
	particle.orbital_speed = particle.initial_orbital_speed;
}

void start() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = vec3(0.0);
	initialize(particle);
	if (rand(particle.seed) > AMOUNT_RATIO) {
		ACTIVE = false;
	}
	if (RESTART_CUSTOM) {
		CUSTOM = vec4(0.0);
		CUSTOM.x = particle.initial_rotation;
	}
	if (RESTART_COLOR) {
		COLOR = particle.initial_color;
	}
	if (RESTART_ROT_SCALE) {
		TRANSFORM[0].xyz = rotated(vec3(1.0, 0.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.x;
		TRANSFORM[1].xyz = rotated(vec3(0.0, 1.0, 0.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.y;
		TRANSFORM[2].xyz = rotated(vec3(0.0, 0.0, 1.0), particle.rotation_axis, particle.initial_rotation) * particle.initial_scale.z;
	}
	if (RESTART_POSITION) {
		TRANSFORM[3].xyz = particle.initial_position;
		TRANSFORM = EMISSION_TRANSFORM * TRANSFORM;
	}
	if (RESTART_VELOCITY) {
		VELOCITY = (EMISSION_TRANSFORM * vec4(particle.initial_velocity, 0.0)).xyz;
	}
}

void process() {
	Particle particle;
	// Use INDEX instead of NUMBER for repeatability.
	uint base_number = NUMBER;
	particle.seed = hash(base_number + uint(1) + RANDOM_SEED);
	particle.delta = DELTA;
	particle.lifetime = LIFETIME;
	particle.index = INDEX;
	particle.number = NUMBER;
	particle.age = 0.0;
	particle.normalized_age = 0.0;
	particle.normalized_index = normalized_by_amount(INDEX);
	particle.normalized_number = normalized_by_amount(NUMBER);
	particle.position = TRANSFORM[3].xyz;
	particle.velocity = VELOCITY;
	particle.orientation = mat3(
		normalize(TRANSFORM[0].xyz),
		normalize(TRANSFORM[1].xyz),
		normalize(TRANSFORM[2].xyz));
	particle.force = vec3(0.0);
	particle.emission_transform = EMISSION_TRANSFORM;
	particle.previous_transform = TRANSFORM;
	particle.rotation = CUSTOM.x;
	particle.just_spawned = CUSTOM.y == 0.0;
	particle.collided = COLLIDED;
	particle.collision_normal = COLLISION_NORMAL;
	particle.collision_depth = COLLISION_DEPTH;
	particle.color = COLOR;
	initialize(particle);
	CUSTOM.y += DELTA / particle.lifetime;
	CUSTOM.y = mix(CUSTOM.y, 1.0, INTERPOLATE_TO_END);
	CUSTOM.y = clamp(CUSTOM.y, 0.0, 1.0);
	particle.age = CUSTOM.y * particle.lifetime;
	particle.normalized_age = CUSTOM.y;
	m3_scale_size(particle);
	m4_scale_color(particle);
	m5_add_rotation(particle);
	if (abs(particle.orbital_speed) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 next = rotated(local_pos, particle.orbital_axis, particle.orbital_speed * particle.delta);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
		particle.orbital_velocity = (next - local_pos) / particle.delta;
	}
	if (abs(particle.radial_velocity) >= 0.01 && particle.delta >= 0.001) {
		vec3 local_pos = (inverse(particle.emission_transform) * vec4(particle.position, 1.0)).xyz;
		vec3 d = local_pos;
		float mag = min(length(d), abs(particle.radial_velocity) * particle.delta);
		vec3 next = local_pos + normalize_or_zero(d) * mag * sign(particle.radial_velocity);
		particle.position = (particle.emission_transform * vec4(next, 0.0)).xyz;
	}
	particle.position += particle.velocity * particle.delta;
	VELOCITY = particle.velocity;
	TRANSFORM[0].xyz = particle.orientation[0] * particle.scale.x;
	TRANSFORM[1].xyz = particle.orientation[1] * particle.scale.y;
	TRANSFORM[2].xyz = particle.orientation[2] * particle.scale.z;
	TRANSFORM[3].xyz = particle.position;
	COLOR = particle.color;
	CUSTOM.x = particle.rotation;
	
	if (particle.age >= LIFETIME) { ACTIVE = false;  }
}

"

[sub_resource type="Curve" id="Curve_0etqu"]
_data = [Vector2(0, 0), 0.0, 2.5109007, 0, 0, Vector2(0.51000005, 0.868065), 0.43978292, 0.43978292, 0, 0, Vector2(1, 1), 0.0, 0.0, 0, 0]
point_count = 3

[sub_resource type="CurveTexture" id="CurveTexture_u7j6j"]
curve = SubResource("Curve_0etqu")
metadata/uniform_name = "u_m3_scale"

[sub_resource type="Curve" id="Curve_6b4aw"]
_data = [Vector2(0, 1), 0.0, 0.0, 0, 0, Vector2(1, 0), 0.0, 0.0, 0, 0]
point_count = 2

[sub_resource type="CurveTexture" id="CurveTexture_ct7xu"]
curve = SubResource("Curve_6b4aw")
metadata/uniform_name = "u_m4_alpha"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_kyo84"]
shader = SubResource("Shader_e5x8v")
shader_parameter/initial_color = Color(1, 0.8065, 0.57, 1)
shader_parameter/u_m3_scale = SubResource("CurveTexture_u7j6j")
shader_parameter/u_m4_alpha = SubResource("CurveTexture_ct7xu")

[sub_resource type="Shader" id="Shader_vbv8k"]
code = "shader_type spatial;
render_mode unshaded, cull_disabled;

uniform float opacity = 1.0;
uniform float emission = 1.0;

uniform sampler2D main_tex : source_color, filter_linear_mipmap, repeat_disable;

varying flat uint instance_id;
varying float age;

void vertex()
{
	instance_id = uint(INSTANCE_ID);
	age = INSTANCE_CUSTOM.y;
	vec4 offset = vec4(0.0);
	mat4 model = mat4(
		normalize(MAIN_CAM_INV_VIEW_MATRIX[0]),
		normalize(MAIN_CAM_INV_VIEW_MATRIX[1]),
		normalize(MAIN_CAM_INV_VIEW_MATRIX[2]),
		MODEL_MATRIX[3] + offset);
	model = model * mat4(
		vec4(cos(INSTANCE_CUSTOM.x), -sin(INSTANCE_CUSTOM.x), 0.0, 0.0),
		vec4(sin(INSTANCE_CUSTOM.x), cos(INSTANCE_CUSTOM.x), 0.0, 0.0),
		vec4(0.0, 0.0, 1.0, 0.0),
		vec4(0.0, 0.0, 0.0, 1.0));
	model = model * mat4(
		vec4(length(MODEL_MATRIX[0].xyz), 0.0, 0.0, 0.0),
		vec4(0.0, length(MODEL_MATRIX[1].xyz), 0.0, 0.0),
		vec4(0.0, 0.0, length(MODEL_MATRIX[2].xyz), 0.0),
		vec4(0.0, 0.0, 0.0, 1.0));
	MODELVIEW_MATRIX = VIEW_MATRIX * model;
	MODELVIEW_NORMAL_MATRIX = mat3(MODELVIEW_MATRIX);
}

void fragment()
{
	vec4 main_s = texture(main_tex, UV);
	ALPHA = COLOR.a;
	ALPHA *= main_s.r;
	ALPHA = clamp(ALPHA * opacity, 0.0, 1.0);
	ALBEDO = COLOR.rgb * emission;
	ALBEDO *= main_s.rgb;
	
}


"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_ex7wl"]
render_priority = 0
shader = SubResource("Shader_vbv8k")
shader_parameter/opacity = 1.0
shader_parameter/emission = 4.0
shader_parameter/main_tex = ExtResource("5_xpi13")

[sub_resource type="QuadMesh" id="QuadMesh_lfv8q"]
material = SubResource("ShaderMaterial_ex7wl")

[node name="Effect" type="Node3D" unique_id=1356064897]
script = SubResource("GDScript_kyo84")
"Fire Color" = SubResource("GradientTexture1D_k04dp")
"Glow Color" = Color(0.96, 0.60144, 0.1632, 1)
"Spark Color" = Color(0.92, 0.7436667, 0.46, 1)
"Flare Color" = Color(1, 0.8065, 0.57, 1)

[node name="Fire" type="GPUParticles3D" parent="." unique_id=1703953469]
cast_shadow = 0
emitting = false
amount = 64
fixed_fps = 60
interpolate = false
fract_delta = false
local_coords = true
process_material = SubResource("ShaderMaterial_unohw")
draw_pass_1 = SubResource("QuadMesh_vmox8")

[node name="Glow" type="GPUParticles3D" parent="." unique_id=2124803860]
cast_shadow = 0
emitting = false
amount = 1
fixed_fps = 60
interpolate = false
fract_delta = false
local_coords = true
process_material = SubResource("ShaderMaterial_c1r1o")
draw_pass_1 = SubResource("QuadMesh_rw7bw")

[node name="Sparks" type="GPUParticles3D" parent="." unique_id=23726095]
cast_shadow = 0
emitting = false
amount = 6
fixed_fps = 60
interpolate = false
fract_delta = false
local_coords = true
process_material = SubResource("ShaderMaterial_832lv")
draw_pass_1 = SubResource("QuadMesh_fgan6")

[node name="Flare" type="GPUParticles3D" parent="." unique_id=1586625649]
cast_shadow = 0
emitting = false
amount = 1
lifetime = 0.3
one_shot = true
fixed_fps = 60
interpolate = false
fract_delta = false
local_coords = true
process_material = SubResource("ShaderMaterial_kyo84")
draw_pass_1 = SubResource("QuadMesh_lfv8q")
